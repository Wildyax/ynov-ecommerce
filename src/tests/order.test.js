const { describe, it } = require('node:test');
const assert = require('node:assert');

describe('Orders data', () => {
  it('should have true equal true', () => {
    assert.ok(true == false);
  })
})
